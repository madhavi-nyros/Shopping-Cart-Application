
<html lang="en">
<head>
	 

 <%#= javascript_include_tag :defaults %>

	
<%= javascript_include_tag 'jquery','easyTooltip','datetimepicker_css' %>

<%= stylesheet_link_tag    "application", :media => "all" %>
       
    <%= csrf_meta_tag %> 

<meta charset="utf-8">
<title>shopping cart products</title>

<script type="text/javascript">
 $(document).ready(function() {


$("#firstpane p.menu_head").click(function()
{
    $(this).css({backgroundImage:"url(down.png)"}).next("div.menu_body").slideToggle(300).siblings("div.menu_body").slideUp("slow");
    $(this).siblings().css({backgroundImage:"url(left.png)"});
});
$("a").easyTooltip();
});

function slideSwitch() {
    var $active = $('#slideshow IMG.active');

    if ( $active.length == 0 ) $active = $('#slideshow IMG:last');

    // use this to pull the images in the order they appear in the markup
    var $next =  $active.next().length ? $active.next()
        : $('#slideshow IMG:first');

    // uncomment the 3 lines below to pull the images in random order
    
    // var $sibs  = $active.siblings();
    // var rndNum = Math.floor(Math.random() * $sibs.length );
    // var $next  = $( $sibs[ rndNum ] );


    $active.addClass('last-active');

    $next.css({opacity: 0.0})
        .addClass('active')
        .animate({opacity: 1.0}, 1000, function() {
            $active.removeClass('active last-active');
        });
}

$(function() {
    setInterval( "slideSwitch()", 5000 );
});

</script>
    

</head>

<body>

<header> <!-- main header bar-->

    <hgroup>
        <h1>Pc4U.com</h1>
        <h2>Choose best with low price</h2>
    </hgroup>
    <nav>
        <ul>
            <li><a href="#">Home</a></li>
            <li><%= link_to "New Products",products_path %></li>
            <li><a href="#">AboutUs</a></li>

<li><a href="/admin">Admin Login</a></li>

            <li><a href = "javascript:void(0)" onclick = "document.getElementById('light').style.display='block';document.getElementById('fade').style.display='block'">JoinUs</a></p>
		<div id="light" class="white_content">This is the lightbox content.
<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
 %>
  <%= devise_error_messages! %>

  <p>Email<br />
  <%= f.email_field :email %></p>

  <p>Password<br />
  <%= f.password_field :password %></p>

  <p>Password Conformation<br />
  <%= f.password_field :password_confirmation %></p>

  <div><%= f.submit "Sign up",:class => "signup-form" %></div>

<% end %>
<a href = "javascript:void(0)" onclick = "document.getElementById('light').style.display='none';document.getElementById('fade').style.display='none'">Close</a>
</div>	
</li>

        </ul>

    </nav>
    <a href="#" title="choose best through buypc4U.com"><%= image_tag('logo.jpg',:width=>'200px',:height=>'100px',:class => "logo")  %></a>

</header>  <!-- end of the header part -->
<div id="slideshow">
    <div>
   <span> <%= image_tag "b1.jpeg", :class => 'active'%>
 <%= image_tag "b2.jpg"  %>
<%= image_tag "b3.jpg"  %></span>
</div>
<div class="save"><%= image_tag "save.gif" %>
</div></div>

<section id="left"> <!-- side bar -->

	<h1>Sign In</h1>
    
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <tr><td>Email
 <%= f.email_field :email %></td></tr>

 <tr><td>Password
  <%= f.password_field :password %></td></tr>
  <% if devise_mapping.rememberable? -%>

   <tr><td>RememberMe <%= f.check_box :remember_me %> 
</td></tr>

</table>
<% end -%>

  <p><%= f.submit "Sign in", :class => 'submit-form' %></p>

<% end %>
	<%= link_to image_tag("fb.jpeg"), '/auth/facebook/', :class => "fb" %>
	<%= link_to image_tag("t.jpg"), '/auth/twitter/' %>

	 
	<h1>Categories</h1>


<div id="firstpane" class="menu_list">
 <%#= render :template => '/categories/new' %>

<%= form_tag new_category_path, method: :get do %>

  <p>
    <%= text_field_tag :query, params[:query] %>
    <%#= submit_tag "Search", categoryname: nil,:class=>"search-form" %>
  </p>

<% if !@categories.blank? %>
<% for item in @categories %>
<table>
<tr>

<p class="menu_head"><span class="selecting"><%= link_to item.categoryname, :controller => :products, :action => :new, :id =>item.id %></span>
</tr>
<% end %>
</table>
<% end %>
<% end %>
</p>
 
</div>
 <h1 class="manufactures">MANUFACUTRES</h1>
<ul>
        <li><a href="#">APPLE</a></li>
        <li><a href="#">SONY </a></li>
        <li><a href="#">LENOVO </a></li>
	<li><a href="#">DELL</a></li>
        <li><a href="#">H.P </a></li>
        <li><a href="#">SAMSUNG </a></li>
        <li><a href="#">ACCER</a></li>
        
       
    </ul>
<a href="#" title="to know more about the latesst products and top rated one's click here">See more..</a> 
</section> <!-- end of the side bar -->
 
<article> <!-- content -->
<%= yield %>


</article><!-- end of the conent -->


<footer>
	<p>&copy; 2012 All Rights Reserved </p>
</footer>

</body>
</html>

